"""agent_system.scorecard: rubric, evaluation lifecycle, legacy migration.

Rubric, weights, and aggregation are preserved exactly from the AgentSystem
scorecard (plan V2 §14.1); CAO is the sole writer (§14.2).
"""
